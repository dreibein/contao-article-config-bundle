<?xml version="1.0"?>
<ruleset name="Dreibein">
    <description>Dreibein ruleset based on PSR-2 but modified for Contao</description>

    <rule ref="PSR2">
        <!-- Contao dca class names are not in camel cpas so ignore these rules  -->
        <exclude name="Squiz.Classes.ValidClassName.NotCamelCaps"/>
    </rule>
</ruleset>